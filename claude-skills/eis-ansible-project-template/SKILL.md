---
name: eis-ansible-project-template
description: Use when scaffolding a new per-customer EIS Ansible project (VM-level config — build hosts, Jenkins, GitLab, SonarQube, Atlantis, Selenoid, Sisense, Keycloak), when the user says "create the ansible project for <customer>", "generate a new ansible repo", "onboard a customer's ansible", "copier copy the ansible template", or when extending/maintaining the iac/ansible/template/client Copier template itself. Also use when an existing generated ansible project needs a `copier update`, or when adding/parameterizing a new customer-specific value into the template. Covers the custom-delimiter rule that keeps Ansible {{ }} intact, per-service enable_* gating, what is vs isn't templated, the GitLab push/CI gotchas, and the E2E parity-vs-CAA validation method.
---

# EIS per-customer Ansible project Copier template

## What it is
`iac/ansible/template/client` (GitLab group `iac/ansible/template`, subgroup id 1717; sibling of `terraform/template/{client,module}`). A Copier template that scaffolds a per-customer Ansible project mirroring `projects/aws/<customer>/ansible`. **Reference impl + extraction source = the Credit Agricole repo** (`projects/aws/credit-agricole/ansible`, code `caa`, us-west-2). Built + shipped **v1.0.1** 2026-06-18, CI green, E2E-validated against CAA (zero unintended drift). **Current release: v1.1.0 (2026-06-29)** — adds the axajp/EISSAASDEV-302 fixes (upgrade:false default, run.sh SSO fallback, SonarQube 25.9, sisense /opt mount, Jenkins CasC myViewsTabBar drop); **render `--vcs-ref v1.1.0`** — v1.0.x predates all of them and produces a broken toolchain.

Key insight that makes it small: the CAA repo is **already ~90% parameterized at Ansible runtime** via `{{ project_name }}` (Vault paths `secret2/data/{{ project_name }}/...`, S3 buckets, AD groups `jnk_{{ project_name }}_ssh`, ASG names). Copier only fills the literals Ansible **cannot** derive at runtime.

## Generate a new project
```bash
copier copy \
  "git+ssh://git@sfo-cvdevopsgit01.eqxdev.exigengroup.com:2224/iac/ansible/template/client.git" \
  ~/gitwork/iac/projects/aws/<customer>/ansible
```
Answers: `project_code` (lowercase 2-6, becomes `project_name`), `full_project_name`, `region` (→ `region_code` auto-computed, **confirm it**), `domain_name` (default `<project_code>-eis.cloud`), `aws_profile`, `vault_addr`, + 12 `enable_*` service flags (jenkins/build_node default true; rest false). Selenoid also asks subnet/SG/IAM-profile ids — leave blank, fill after the VPC exists. After generation: `ansible-galaxy install -r roles/requirements.yml`; `cp .env.example .env` + edit `VAULT_TOKEN`; macOS → `docker build -t ansible-aws:local docker/` then `./docker/run.sh shell`.

Update an existing project: `cd <project>; copier update --defaults .` (answers live in `.copier-answers.yml`, regenerated — never hand-edit).

## The one non-negotiable design rule — custom delimiters
Ansible YAML contains `{{ }}` / `{% %}` that must reach generated files **verbatim** (Ansible evaluates them at runtime). So Copier markers are `[[ ]]` / `[% %]` / `[# #]` (set in `copier.yaml` `_envops`, same as `terraform/template/{client,module}`). Result: `aws_ssm_bucket_name: "{{ aws_region_code}}{{ project_name }}ansible"` passes through untouched; only `project_name: "[[ project_code ]]"` is rendered by Copier. `_templates_suffix: ""` (scan all files), `_subdirectory: template`, `trim_blocks/lstrip_blocks: true`.

## What IS templated (literals Ansible can't derive) vs static
- **Templated:** `group_vars/all.yml` `project_name`/`aws_region`/`aws_region_code`; `inventory/aws_ec2.yml` SSM bucket + region (**dynamic inventory loads BEFORE group_vars → cannot use Ansible vars, must be a Copier literal**); `group_vars/infra.yaml` DNS zone + acme wildcard domains; Keycloak/Sisense hostnames; `host_vars` **filename** (`[[ region_code ]][[ project_code ]]keycloak01.yaml`); `.env.example` + `docker/run.sh` defaults (profile, vault addr); Selenoid subnet/SG/IAM-profile (when `enable_selenoid`).
- **Static / runtime-derived (NOT templated):** everything keyed off `{{ project_name }}` (Vault paths, buckets, AD groups, ASG names); `ansible.cfg`; base playbooks; `docker/Dockerfile`; the committed `local_kernel_patch` role.
- **Generalizations applied during extraction** (render identical values for CAA, but portable): `ta.yaml` `project_sonar`/`selenoid_asg_name` → `{{ aws_region_code }}{{ project_name }}…`; `all.yml` `selenoid_env: AWS0-…` → `{{ aws_region_code | upper }}-…`; `selenoid_ticket` blanked.

## Per-service gating
Each `enable_<svc>` flag gates a playbook + its `group_vars` + its `roles/requirements.yml` entry, via **Jinja conditional file/dir names** with the custom delimiters: `playbooks/[% if enable_gitlab %]gitlab.yaml[% endif %]`, `inventory/group_vars/[% if enable_build_node %]bld.yaml[% endif %]`, dir `playbooks/[% if enable_jenkins %]jenkins[% endif %]/`. When false the name renders empty → Copier skips the file/dir entirely. `requirements.yml` wraps each role's `- src:` block in `[% if enable_x %]…[% endif %]`; base roles (environment, docker_install, vault_agent_ssl, rhel_subscription, acme_cert_update, linux_joindomain) always present. Service→role map: jenkins→docker_compose_jenkins, build_node→temurin_openjdk+maven+ant_install+cli_tools, gitlab/sonar/opengrok/atlantis/selenoid→docker_compose_*, sisense→sisense_install. (nexus/etcs/s_hub gate the **playbook only** — no role in requirements; matches CAA source.)

## Roles are external; .env is secret
Only `roles/requirements.yml`, `roles/.gitignore`, and the committed `local_kernel_patch` role ship. Other roles install via `ansible-galaxy install -r roles/requirements.yml` (the `roles/.gitignore` = `/*` ignores them). `.env` is gitignored in the source → the template ships `.env.example` (no token).

## Maintaining / extending the template
- Edit under `template/` using `[[ ]]`/`[% %]`. Add a new var to `copier.yaml`; reuse the `region_code` dict-default idiom (copied verbatim from `argocd/template/clusters`) and `validate: "^regex$"` string idiom (from `terraform/template/client`).
- **Validate locally**: `bash ci/mock-test.sh` — renders `ci/mock-answers.yml`, greps for leaked `[[`/`[%`/`[#` markers, parses all YAML. Needs the dir to be a git repo (uses `--vcs-ref HEAD`) → commit first.
- Bump a tag for every release (`v1.0.x`); consumers pin `--vcs-ref`. Don't move a published tag.
- Reviewer routing: **ansible → `--reviewer eramadan`** (see [[feedback_mr_reviewer_routing]]).

## GitLab push / CI gotchas (hit on first ship, 2026-06-18)
1. **`glab repo create iac/ansible/template/foo` 404s** ("Could not find group ansible") — glab mis-parses the nested path, AND `iac/ansible/template` already exists as a **subgroup**, not a project. Create the project via the API with an explicit namespace id:
   `glab api --method POST projects -f name=client -f path=client -F namespace_id=1717 -f visibility=private`
   then add the remote manually: `ssh://git@sfo-cvdevopsgit01.eqxdev.exigengroup.com:2224/iac/ansible/template/client.git`.
2. **Pipeline stuck `pending` forever** — every group runner is `run_untagged:false` (`glab api runners/<id>` → check `run_untagged`/`tag_list`). An untagged job NEVER runs. Add `tags: [terraform]` to the CI job (runners 16/32 are the `terraform`-tagged k8s-executor runners in the `iac` namespace and run arbitrary docker images, e.g. `python:3.12-slim`). Sibling templates pin the same tag.
3. **Generated-project pre-commit** ships end-of-file-fixer + trailing-whitespace + yamllint (ansible-lint commented until galaxy roles installed). The CAA source had error-level yamllint hits (no-newline-at-EOF, trailing spaces) → fix them in the template source (`perl -i -pe 's/[ \t]+$//'` + `perl -i -0777 -pe 's/\s*\z/\n/'`) so generated projects pass their own pre-commit on first run. Cosmetic indentation/blank-line **warnings** are non-failing (yamllint exits 0 on warnings) — leave them; they match upstream.

## E2E validation (parity vs the reference customer) — the proof method
Pull the **published tag** (not the local working dir) and diff against the real customer tree, read-only:
1. `copier copy --defaults --data-file <caa-answers.yml> --vcs-ref v1.1.0 <git-url> $TMP/render` (answers = the customer's real values incl. selenoid ids + all services on).
2. Export the live customer tree **without touching it**: `git -C <caa-repo> archive HEAD | tar -x -C $TMP/caa` (read-only; never push/modify the customer repo). Add any untracked-but-real files (e.g. `sisense_cert.yaml`) for parity.
3. `diff -rq $TMP/caa $TMP/render` for structural drift; `diff -ru …` for content.
**Pass = zero files missing; only intentional additions** (`.copier-answers.yml`, `.env.example`, `.pre-commit-config.yaml`); **and every content diff is one of:** docs/cosmetic, the v1.0.1 whitespace cleanup, runtime-`{{ }}` generalizations that resolve to identical values (`{{ aws_region_code }}{{ project_name }}` with aws0/caa = `aws0caa…`), or the requirements.yml base-first reorder (same roles/versions). Confirm the Ansible `{{ }}` survived: `grep -rn '{{ project_name }}\|secret2/data' $TMP/render/inventory` should match the source byte-for-byte. The v1.0.1 E2E run confirmed all 13 content diffs benign, 0 unintended. Clean up `$TMP` after.

## Vault access + run prerequisites (axajp run — verified EISSAASDEV-302)
Before running ANY playbook, the Vault path must be **access-granted AND fully populated** — these are
THREE separate gates, not one. Diagnose by HTTP code (see below); each points at a different owner.

- **Path = `secret2/data/<project_code>`** (new-client convention, NOT `rnd/cicd/3.0/...`). Standing it
  up is **4 distinct things, distinct owners** (see Phase-0 skill §8 for the full breakdown): (1) branch
  — Denys; (2) **AD-group READ grant** — `Genesis_DevOps_CI`/`genesis-ci` policy must include
  `secret2/data/<proj>/*` (needs approver sign-off); (3) **skeleton seed** (script) — Sergii/Olha; (4)
  **FULL secret population** — the operational secrets below, several client-specific (cloud/AD team).
- **Diagnose the gate by HTTP code on a direct leaf read** (`curl -o /dev/null -w '%{http_code}'`):
  - **403 `permission denied`** → gate 2 (AD-group grant) missing. **LIST can still succeed** — never
    judge access by LIST. Escalate the grant.
  - **404 `{"errors":[]}`** → access IS granted, but **no data there** → gate 3/4 (skeleton-only or not
    populated). Escalate the seed/populate.
  - **200 + non-empty `.data.data`** → ready.
  ```bash
  VT=$(grep '^VAULT_TOKEN=' .env|cut -d= -f2-); VA=$(grep '^VAULT_ADDR=' .env|cut -d= -f2-)
  curl -sk -o /tmp/v.json -w '%{http_code}\n' -H "X-Vault-Token: $VT" \
    "$VA/v1/secret2/data/<project_code>/automation/ldap"   # 403 vs 404 vs 200 — that's the diagnosis
  ```
- **Diff against the CAA reference** to get the exact missing list: you can usually read
  `secret2/metadata/caa`. `LIST secret2/metadata/caa/automation` → ~29 keys (ldap, gitlab, jenkins,
  nexus, …); a skeleton-seeded client shows only `datadog`. Hand DevOps the precise diff.
- **EXACT leaf paths + keys the playbooks read** (from `inventory/group_vars/all.yml` — lazy Jinja
  lookups, so a playbook fails only when it references the var):
  | Vault leaf (`secret2/data/<proj>/…`) | keys | consumed by |
  |---|---|---|
  | `automation/ldap` | `adhost`,`adsearch`,`adbinddn`,`adpassword` | linux_joindomain |
  | `identities/cicd_team/cicd/default_build_user` | `build_user_helm_url`,`build_user_name`,`build_user_password` | nexus/helm |
  | `identities/cloud_team/cicd/redhat` | (whole secret) | rhel_subscription |
  | `identities/cloud_team/software/sslupdate-certificates-approle` | `role_id`,`secret_id` | vault_agent/acme |
  | `identities/cloud_team/software/ssosync_bind_user` | `username`,`password` | domain-join bind |
  | `ssl/<project_zone>/ecdsa` | (TLS cert) | keycloak/sisense host_vars |
- **Prep while you wait on Vault** — these need only SSO + Docker, NOT Vault, so stage them so the run
  is instant the moment data lands: build `ansible-aws:local` (`docker build -t ansible-aws:local docker/`);
  `ansible-galaxy install -r roles/requirements.yml` (16 roles — see keyscan bug below);
  `aws ssm describe-instance-information --profile <proj> --region <r>` (confirm all toolchain hosts
  PingStatus=Online); `./docker/run.sh exec ansible-inventory -i inventory --graph` (`exec` mode runs
  before the playbook path; still needs `VAULT_TOKEN` set for the wrapper guard, but doesn't read Vault).
  Set **`enable_selenoid=false`** if the Selenoid ASG was deferred in Phase 3.
- **`VAULT_TOKEN`** lives in `.env` (gitignored — `chmod 600 .env`; NEVER commit/echo it). Wrapper
  requires it set (`run.sh` line `: "${VAULT_TOKEN:?}"`).

## ⚠️ `docker/run.sh` ssh-keyscan bug — fresh galaxy install aborts
The wrapper pre-seeds known_hosts but **keyscans only `sfo-devopsgit01`**, while `roles/requirements.yml`
pulls `linux_joindomain`, `sisense_install` and `docker_compose_selenoid` from the **other** host
`sfo-cvdevopsgit01` (also port 2224). On a clean checkout, `ansible-galaxy install -r roles/requirements.yml`
through the wrapper dies with `Host key verification failed` on the first cvdevopsgit01 role and aborts
the rest. **Fix (one line — `ssh-keyscan` takes multiple hosts on one port):**
```sh
ssh-keyscan -p 2224 sfo-devopsgit01.eqxdev.exigengroup.com sfo-cvdevopsgit01.eqxdev.exigengroup.com >> ~/.ssh/known_hosts 2>/dev/null;
```
Fixed in the template + axajp project (MRs, reviewer eramadan, 2026-06-22). The container is `--rm` with
known_hosts rebuilt from the keyscan each run, so re-running with `--force` is a clean end-to-end verify
(all 16 roles install, zero host-key failures). See [[eissaasdev302_axajp_env_state]].

## Learnings — axajp Phase-5 toolchain run (2026-06)
Per-service `docker_compose_*` roles on a fresh env — gotchas beyond the Vault-access gates above:
- **`upgrade: true` in `group_vars/all.yml` LEAVES A FRESH TOOLCHAIN DOWN (the #1 trap; template ships it true).** `upgrade=true` means "redeliver compose/config only, skip init + container start" — the gitlab/jenkins/nexus start handlers are gated `when: not (upgrade | bool)`. On a fresh host this SKIPS compose-up entirely, yet the playbook reports `failed=0 changed=2` (just config render) — a no-op masquerading as success. **Set `upgrade: false` for a fresh install** (role defaults ARE false; only the template all.yml forces true). `sonar` uses its own `sonar_upgrade` (image-diff) and `sisense` uses `sisense_install_method`/`sisense_rke2_present` — both ignore the global flag (that's why they come up fine while gitlab/jenkins/nexus stay down). Flip to true only for code-only redelivery to an already-running host. **Always `docker ps` / curl 443 to confirm — never trust `ok=N changed=N`.**
- **Idempotency notify trap when re-running after a broken `upgrade=true` run.** Start handlers fire only on `notify` (config CHANGE). If the broken run already rendered the compose files, the `upgrade=false` re-run sees them unchanged → no notify → handler skipped AGAIN. Fix: `docker compose up -d` directly via SSM in `/opt/docker_compose_<svc>` (files are already correct). Jenkins is worse — its `init.yml` runs, applies CasC, then **shuts the init containers down** and the normal-start handler is unnotified → manually `docker compose up -d` the normal stack. (A clean fresh install with `upgrade=false` from the start renders+notifies once → no manual step.)
- **Jenkins stuck on the Unlock wizard / private realm despite a correct-looking CasC = the CasC threw a boot failure (axajp 2026-06-26).** JCasC applies the whole document **atomically** — ONE invalid/deprecated key aborts the entire config (`ConfigurationAsCodeBootFailure`), so the `activeDirectory` realm + `globalMatrix` authz never apply and Jenkins falls back to `HudsonPrivateSecurityRealm` + the setup wizard. axajp's casc had a deprecated global `myViewsTabBar: "standard"` → `ConfiguratorException: 'myViewsTabBar' is deprecated`. **Diagnose:** run the init compose and `docker logs jenkins_init 2>&1 | grep -i ConfiguratorException` — it names the offending key (deprecated key, or schema drift vs the installed plugin/core version). **Fix:** remove the bad key from `playbooks/jenkins/casc_config.yaml.j2` (template line ~97). **Apply on a running host without a full playbook re-run:** sed-del the line from the host's rendered `/opt/docker_compose_jenkins/jenkins/casc_config.yaml` → `docker compose -f docker-compose.init.yaml up -d` (init applies CasC to the shared `jenkins_home` volume — config.xml persists) → `down` init → `docker compose up -d --force-recreate` the normal stack → verify `grep securityRealm /var/jenkins_home/config.xml` shows `ActiveDirectorySecurityRealm`. Then `touch /opt/docker_compose_jenkins/.init_done` to lock the state (init block gated `when: not init.stat.exists`). Removing `.init_done` alone does NOT re-trigger init if the casc render is byte-identical — the init block's `notify: init` fires only on a config CHANGE. Normal jenkins compose needs no `CASC_JENKINS_CONFIG` — config.xml persists across recreates, so CasC applies once at init.
- **Sisense `sisense_install` "installation completed" ≠ the app works (axajp 2026-06-26).** The installer validator can pass while the analytics app is dead: the core RKE2 pods (`identity`, `query`, `build`, `galaxy`, `management`, `intelligence`, `ae`, `ai-*`, `exporting`, `external-plugins`, `model-graphql`, `plugins`) sit in **`Init` forever**. Cause: `identity`'s `check-activation` init gates on `oxygen.sisense.svc:31112/get` returning HTTP **200**; if **`oxygen` (Running 1/1) returns HTTP 500** continuously, the system is **NOT activated** (license/activation step never succeeded) → identity never starts → everything cascades. **Not infra, not a restart fix** — needs Sisense product activation (license + maintainer). Verify post-install (don't trust the validator): SSM `*sis01` → `sudo /var/lib/rancher/rke2/bin/kubectl --kubeconfig /etc/rancher/rke2/rke2.yaml -n sisense get pods | grep Init` should be EMPTY; if not, check the identity `check-activation` logs + `wget -S oxygen:31112/get` from a running pod. Full diagnosis in skill `eis-onesuite-e2e-verify` §9.
- **`docker/run.sh` SSO-expired-in-container.** Long sessions: the SSO access token expires; the container can't refresh it (no browser) → `aws_ec2` dynamic inventory fails with `Passing both a profile and access tokens is not supported` / `SSO session expired` → **0 hosts → playbooks no-op** (`skipping: no hosts matched`, empty PLAY RECAP). Host `sts`/SSM still work off cached role creds, so it's deceptive. FIX (now in run.sh): when static creds are exported on the host (`eval $(aws configure export-credentials --profile <p> --format env)`), pass `-e AWS_ACCESS_KEY_ID/SECRET/SESSION_TOKEN` and **drop** `-e AWS_PROFILE` (boto3 rejects profile + creds together). Cheap re-auth alternative: `aws sso login --profile <p>` (interactive).
- **Registry-mirror gap is FLEET-WIDE, not per-service.** `docker compose up` images come from `cvops-reg01.eisgroup.com:5000` (Vault `automation/registry` → `registry_host`). For a new account the mirror is MISSING the toolchain images for that account's (shared) `docker_puller` → `failed to resolve reference … not found` → **no containers, service silently not running** (playbook can still rc=0; check `docker ps`, don't trust ok=N). Confirmed missing for axajp: `gitlab/gitlab-ce:18.0.1-ce.0`, `sonarqube:<ver>-community`, `bitnami/nginx-ldap-auth-daemon-archived`. **FIX = override each `docker_image_*` to a PUBLIC source** in group_vars (all images are public): `docker.io/library/sonarqube`, `docker.io/bitnamilegacy/nginx-ldap-auth-daemon-archived` (bitnami sunset → bitnamilegacy org, exact names), `gitlab/gitlab-ce`, `jenkins/jenkins`, `sonatype/nexus3`, `keycloak/keycloak`. OR get them mirrored (COEXT registry ticket, fleet-wide). `opengrok/docker` already pulls from Docker Hub (no registry prefix) — never the blocker. Verify a tag exists before overriding: `docker manifest inspect docker.io/<img>:<tag>`.
- **Registry FIXED → REVERT the bypass + adopt fleet Sonar (2026-06-26, COEXT-106092 resolved).** Once PnT mirrors the images, REVERT each `docker_image_*` override back to the `cvops-reg01` role default (the proper solution). **Sonar:** bump `docker_compose_sonar` v1.0.0→**v1.3.0** + `sonar_version`→**25.9.0.112764** (PnT/all-projects standard) + drop the `docker_image_sonar` override (role default = `cvops-reg01:5000/sonarqube:<ver>-community`). **Sonar-upgrade gotchas:** (a) plugins are version-pinned tarballs on the **SHARED** nexus `nexus_url=eqx-cvops-reg01.eqxdev.exigengroup.com` (NOT the project nexus) at `/repository/packager/public/sonar_plugins_<ver>.tar.gz`, auth = `docker_puller` (puller_login/puller_password) — `curl -u` it (expect 200) BEFORE upgrading, else the upgrade's plugin step 404s; (b) v1.3.0 defaults `sonar_patch_branch_plugin: true`, which pulls `sonarqube-webapp.zip` from github per sonar version (fragile external dep) → set **false** for a clean install; (c) **back up first**: `docker exec <sonar-postgres-ctr> sh -c 'pg_dump -U $POSTGRES_USER -d $POSTGRES_DB' > backup.sql` (uses container env, no cred exposure); (d) after the image bump Sonar returns **`DB_MIGRATION_NEEDED`** → `curl -X POST https://localhost/api/system/migrate_db` then poll `/api/system/status` to `UP` (the role's availability check may not trigger it); (e) compose recreate can throw a transient `removal of container ... is already in progress` (failed=1) — the stack still comes up; an idempotent re-run clears it (sonar_upgrade=false once the image matches). **Opengrok:** the `docker_compose_opengrok` role default `nginx_ldap_name` is the DEAD `bitnami/nginx-ldap-auth-daemon-archived` path — override to `bitnamilegacy/nginx-ldap-auth-daemon-archived` (bitnami sunset Aug-2025 → relocated to the `bitnamilegacy` org, which the mirror carries). Shipped: axajp ansible !9 + client-template !4.
- **Nexus proxy can't pull upstream → `not found` / `401`, even though the repos exist + show "Online" (axajp 2026-06-30, COEXT-106243).** Two INDEPENDENT root causes, both masked because the `docker_compose_nexus` role finishes **green**: **(1) Upstream proxy account missing.** The `gennexus-genesis-{docker,npm,mvn}` proxies auth to upstream `sfoeisgennexus01.exigengroup.com` as **`nexus_<code>-proxy`** (pw seeded by us in Vault `secret2/data/<code>/identities/cicd_team/cicd/default_proxy_user`). If OPS hasn't **created** that account upstream + granted **`nexus_genesis-members`**, every fetch = **401** → the client group (`<code>-docker`, port 5000) serves `not found`. **OPS must create it via EISHELP, mirroring the working `nexus_caa-proxy`** — file day-0 (now in `eis-onesuite-external-deps` register). **(2) Nexus 3.92.x SSRF protection** blocks proxy upstreams resolving to a **private IP** — `sfoeisgennexus01` → `10.23.172.34` → proxy repo silently dead. The role's proxy/group creates use `status_codes:[201,400]` so an SSRF-block (or any 400) is **swallowed as success**. **Role fixes to ship:** (a) set SSRF allowance/allowlist for trusted internal upstreams BEFORE creating proxy repos; (b) drop `400` from create `status_codes`; (c) add a post-create validation that pulls through the group and **fails loud**. **Verify (never trust ok=N):** `docker login <nexus>:5000` → `docker pull <nexus>:5000/<genesis-img>` as **admin** for ground truth (a non-privileged build_user gets **403**, masking the real 404; the privileged account gets `manifest unknown`/404). REST: `…/service/rest/v1/repositories` + `:5000/v2/_catalog` (basic auth OK); global HTTP forward-proxy via extdirect `coreui_HttpSettings` (no v1 REST). [[axajp_nexus_proxy_upstream_401]]
- **vault-agent ships DISABLED → HTTPS dead fleet-wide.** Every nginx front needs `/opt/ssl/{certificate.cer,private.key,ca.cer}`, rendered by **vault-agent** (auths via `sslupdate-certificates-approle`). The role installs the config but leaves the service `disabled`/dead → `/opt/ssl` empty → nginx `cannot load certificate` crash-loop + `/opt/vault/.vault-token` missing → acme deploy 403. **The approle itself is fine** (don't chase permissions). FIX: `systemctl enable --now vault-agent` on each host (→ "authentication successful"). The vendored `vault_agent_ssl` role / `vault_agent.yaml` SHOULD enable+start it — gap. Stopgap if vault-agent's render lags: place the seeded cert at `/opt/ssl` manually.
- **Don't run `sonar.yaml`/`opengrok.yaml` etc. `hosts: all` unscoped** — they share `/opt/nginx/nginx.conf`, so running multiple on the same host COLLIDES the config (`host not found upstream opengrok:8080`). Run **`--limit <service-host>`**. If you already collided them: `docker compose down` the wrong stacks + re-run scoped; a stranded nginx with empty docker network → `docker compose down && up` to rejoin.
- **`automation/ldap` needs BOTH key families** — not just the `ad*` set in the table above:
  - `ad*` (`adhost`,`adbinddn`,`adpassword`,`adsearch`) → `linux_joindomain` domain-join.
  - `ldap*` (`ldaphost`,`ldapsearch`,`ldapbinddn`,`ldappassword`,`ldapprotocol`,`ldaprolebasedn`,`ldapsearchbase`,`ldapuserbasedn`,`ldapuserrequest`,`ldapusersearch`) → `docker_compose_gitlab`/`sonar`/`opengrok` APP LDAP auth.
  - Seeding only `ad*` → gitlab/sonar/opengrok templating fails: `secret ...automation/ldap does not contain the field 'ldaphost'`. Copy the shared `ldap*` set from `secret2/data/caa/automation/ldap` (shared LDAP service account).
- **`opengrok`** (`docker_compose_opengrok`) reads `identities/cicd_team/cicd/default_build_user:build_user_git_token` (a shared `glpat`) — DISTINCT from `build_user_password`; if missing, `git-credentials.j2` render fails. Copy the shared glpat from caa `.../default_build_user`.
- **build_node maven over SSM EXEC timeout** — maven 3.8.9 is pulled from slow `archive.apache.org`; the `unarchive` over `community.aws.aws_ssm` overruns the default 60s EXEC timeout → host UNREACHABLE (`timeout on host ... AnsiballZ_unarchive`). Fix: `ansible_aws_ssm_timeout: 600` in `inventory/group_vars/bld.yaml`.
- **Vault writes — token may lack `patch` capability** (PATCH → 403). Use read-modify-write **POST** to `secret2/data/<path>` (create/update) instead of merge-patch PATCH.
- **TLS cert (`acme_cert_update`) has TWO failure layers:**
  1. **render chicken/egg** — meta-dep `vault_agent_ssl` "Render SSL manually" hard-fails on the not-yet-existing `ssl/<project_zone>/ecdsa`. FIX: pre-seed a self-signed ECDSA wildcard there (openssl + write full/key/ca/cert).
  2. **deploy** — `acme --issue` SUCCEEDS (DNS-01 via Route53) but the `vault_cli_eis` deploy hook reads `VAULT_TOKEN=$(</opt/vault/.vault-token)`. TWO things gate it: (i) the approle (`sslupdate-certificates-approle`) needs write on `secret2/data/<proj>/ssl/*` — a Vault-admin grant (axajp = GENESIS-431673, fixed); (ii) the **token sink**. `acme_cert_update`'s `tasks/main.yml` inserts a `sink "file" { path = /opt/vault/.vault-token }` into vault-agent's `agent-config.hcl` then `flush_handlers` — BUT the `vault_agent_ssl` start handler is `state: started` (NO-OP if vault-agent is already active) so the sink never activates and no token is written. **You MUST `systemctl restart vault-agent`** (not start) → log `agent.sink.file: token written`. **WORKING real-cert recipe (verified axajp 2026-06-25, all 6 serving hosts flipped seed→LE):** run the acme play on `*jnk01`; `systemctl restart vault-agent` (token lands); on jnk01 `cd /usr/share/acme.sh/acme.sh-<ver> && source ./acme.sh.env && sh /bin/acme.sh --deploy -d '*.<zone>' --deploy-hook vault_cli_eis --ecc` (run MANUALLY — the play's deploy is gated `when: item.rc==0` so a re-run with valid certs (rc=2) skips it; acme strips the `*.` so it writes `secret2/<proj>/ssl/<zone>/ecdsa` keys ca/cert/full/key = the template's `.Data.data.{ca,full,key}`); then `systemctl restart vault-agent` on EACH serving host (KV v2 has no lease → a host that read the seed at startup won't auto-flip) → renders `/opt/ssl` LE + nginx-reload. **Watch for a `dns_zone` Vault key set wrong** (`secret2/data/<proj>/ssl/dns_zone:dns_zone`) — `project_zone`/`project_ssl_path` derive from it; a bad value (e.g. "test") makes the NEXT ansible render point at an empty path → breaks HTTPS fleet-wide. Durable fixes: handler→`restarted`; seed/validate `dns_zone`.
- **sonar+opengrok compose registry pull misses** — may fail `failed to resolve reference cvops-reg01.eisgroup.com:5000/<img>: not found` for `sonarqube:<ver>-community` and `bitnami/nginx-ldap-auth-daemon-archived` — registry content/RBAC gap (shared `docker_puller`; other images pull fine), NOT a config bug → file a registry ticket. Note `docker_install` logs into `cvops-reg01.eqxdev.exigengroup.com` while compose refs `cvops-reg01.eisgroup.com:5000` (possible host mismatch).
- **Play order: `linux_joindomain` BEFORE `docker_install`** — `docker_install` `docker_users` adds AD users → needs SSSD/domain-join first.

## Related
[[ansible_copier_template]] (memory), [[clusters_template_v107]] (copier conditional-dir gating tricks), [[eis-account-vending]] / generate-new-project (the terraform half of onboarding), [[ansible_docker_runner_macos]] (the docker/run.sh runner the generated project uses), [[gitlab_module_repo_bootstrap]].
